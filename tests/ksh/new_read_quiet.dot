for string in '(Cvgd)' '(vgd)';do
   if grep -q "${string}" ${test}.out;then
      result_o=' notok'
      echo "   Message(s) with ${string} should not be present and it is"      	   
   fi
   if [ ${result_o} = notok ];then
      echo "      See test listing  ${PWD}/${test}.out"   
   fi
done
   