export VGRID_HOME=${VGRID_HOME:-}

# Path to disc mounted on frontend and backend (../ords/...)
export VGRID_SHARED_DISK=${VGRID_SHARED_DISK:-${HOME}/ords/maestro}
export VGRID_WORKDIR=${VGRID_SHARED_DISK}/${SEQ_EXP_NAME}/${SEQ_DATE}

export VGRID_SSM_RPN_UTILS=${VGRID_SSM_RPN_UTILS:-"-d /fs/ssm/eccc/mrd/rpn/utils/16.0-beta"}
